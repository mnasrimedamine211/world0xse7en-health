{
  "name": "healthcare-ai-app",
  "version": "1.0.0",
  "description": "Full-stack Healthcare AI Web Application with React frontend and Node.js backend",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "install:all": "echo 'Installing all dependencies...' && npm install",
    "postinstall": "echo '✅ All dependencies installed successfully!'",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "npm run build:client",
    "build:client": "cd client && npm run build",
    "start": "npm run start:server",
    "start:server": "cd server && npm start",
    "test": "npm run test:server && npm run test:client",
    "test:server": "cd server && npm test",
    "test:client": "cd client && npm test",
    "lint": "npm run lint:server && npm run lint:client",
    "lint:server": "cd server && npm run lint || true",
    "lint:client": "cd client && npm run lint || true",
    "clean": "rm -rf node_modules server/node_modules client/node_modules server/uploads client/dist",
    "clean:install": "npm run clean && npm run install:all"
  },
  "keywords": [
    "healthcare",
    "ai",
    "medical",
    "react",
    "nodejs",
    "mongodb",
    "express",
    "symptom-checker",
    "appointment-system"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}

